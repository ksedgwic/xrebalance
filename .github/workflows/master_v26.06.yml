name: master on CLN v26.06.x

on:
    push:
        branches:
            - master
    pull_request:
    workflow_dispatch:

jobs:
    call-ci:
        uses: ./.github/workflows/ci.yml
        with:
            cln-version: "v26.06.1"
            pyln-version: "26.6.1"
